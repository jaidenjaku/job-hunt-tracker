name: Daily Job Scraper

on:
  schedule:
    - cron: '30 12 * * 1-5'   # 8:30 AM Eastern (UTC-4, summer) Mon–Fri
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Random delay
        run: sleep $((RANDOM % 1800))   # sleeps 0–30 minutes

      - name: Run scraper
        env:
          SMTP_ADDRESS: ${{ secrets.SMTP_ADDRESS }}
          EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          RECIEVER: ${{ secrets.RECIEVER }}
          FRIEND_EMAIL: ${{ secrets.FRIEND_EMAIL }}
          PORT: ${{ secrets.PORT }}
        run: python main.py

      - name: Commit updated database
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add listings.db
          git diff --staged --quiet || git commit -m "Update job listings db [automated]"
          git push
